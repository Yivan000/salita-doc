title: Salita
description: A dictionary that contains *all* words from *all* languages.

#theme: cayman
remote_theme: pages-themes/cayman
plugins:
  - jekyll-remote-theme # add this line to the plugins list if you already have one
  - jekyll-include-cache
show_downloads: true

# SITE CONFIGURATION
baseurl: "/salita-doc/docs"
url: "https://yivan000.github.io"

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"